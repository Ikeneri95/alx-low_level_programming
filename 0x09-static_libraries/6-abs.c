#include "main.h"
/**
 * _abs - Return the absolute value
 * @n: number to check the absolute value
 *
 * Return: absolute value
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}
